services:
  postgres-test:
    image: postgres:16-alpine
    container_name: alertup-postgres-test
    environment:
      POSTGRES_USER: postgres
      POSTGRES_PASSWORD: postgres
      POSTGRES_DB: alertup_test
    ports:
      - "5433:5432"
    tmpfs:
      - /var/lib/postgresql/data
